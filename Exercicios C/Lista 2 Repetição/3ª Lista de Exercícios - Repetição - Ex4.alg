algoritmo "ex4"
var num, cont, soma : inteiro
inicio
      soma <- 0
      num  <- 1
      para cont <- 1 ate 99 faca
           soma <- soma + num
           num  <- num + 2
      fimpara
      escreval("A soma dos 100 primeiros números ímpares é: ", soma)
fimalgoritmo
